import * as vscode from "vscode";
import * as fs from "fs";
import * as path from "path";

export default function compileDocs(): void {
    vscode.window.showInformationMessage('Hello world shikibu');
}